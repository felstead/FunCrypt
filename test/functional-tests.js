var assert = require('assert');
var funcrypt = require('../src');
var _ = require('underscore');

describe('Functionals', function () {
    it('should be able to convert a string to blocks', function () {
        
    });
});
